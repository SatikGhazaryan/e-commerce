'use strict';

// const { UserService } = require('../service/user-service.js');

class UserController {
    // static async registration(req, res) {
    //     this; try {
    //         const { email, passwordUser } = req.body;
    //         const userData = await UserService.registration(email, passwordUser);
    //         res.cookie('refreshToken', userData.refreshToken, { maxAge: 30 * 24 * 60 * 60 * 1000, httpOnly: true });
    //         return res
    //             .json(userData);
    //     } catch (error) {
    //         console.log(error);
    //     }
    // }
    // static async postLoginUser(req, res, next) {
    //     this; try {

    //     } catch (error) {

    //     }
    // }
    // static async postLogoutUser(req, res, next) {
    //     this; try {

    //     } catch (error) {

    //     }
    // }
    // static async getActivateUser(req, res, next) {
    //     this; try {

    //     } catch (error) {

    //     }
    // }
    // static async getRefreshUser(req, res, next) {
    //     this; try {

    //     } catch (error) {

    //     }
    // }
}

module.exports = { UserController };
